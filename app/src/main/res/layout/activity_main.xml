<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".MainActivity">
    <android.support.v4.view.ViewPager
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_alignParentTop="true"
            android:layout_alignTop="@+id/navigation_bar"
            android:id="@+id/main_viewPager"
    />
    <android.support.design.widget.TabLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:tabIndicatorColor="@android:color/transparent"
            app:tabIconTintMode="src_in"
            app:tabUnboundedRipple="true"
            android:id="@+id/navigation_bar"
            android:layout_alignParentBottom="true"
            android:background="@color/dark_background_color"
            app:tabTextColor="@color/light_background_color"
            app:tabIndicatorHeight="0dp">

        <android.support.design.widget.TabItem
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/home"
                android:icon="@drawable/ic_tab_home"/>

        <android.support.design.widget.TabItem
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/trending"
                android:icon="@drawable/ic_tab_trending"/>

        <android.support.design.widget.TabItem
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/subscriptions"
                android:icon="@drawable/ic_tab_subscriptions"/>

        <android.support.design.widget.TabItem
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/library"
                android:icon="@drawable/ic_tab_library"/>
    </android.support.design.widget.TabLayout>
</RelativeLayout>